상위: [00 개요 — 전체 구조](/tech/notes/00) · 선행: [17 Wave B — wiki-backend 구현기록, 과정과 원리 해설 (2026-07-21)](/tech/notes/17), [15 ALM·Wiki 백엔드 요구사항 + 서비스 분할 설계 (2026-07-19)](/tech/notes/15) · 후속: [22 wiki-front 컨플루언스 복제 리뉴얼 + 폴더·초안·컬럼 콘텐츠 모델 (2026-07-26)](/tech/notes/22)

## 한눈에 보기

[17번](/tech/notes/17)에서 만든 wiki-backend에 **wiki-front를 처음으로 실제로 붙였다.** 그때까지 프론트는 localStorage 목업 위에서만 돌아갔다.

핵심은 "한쪽을 버리지 않았다"는 것이다. 목업을 지우고 백엔드로 갈아탄 게 아니라, **환경변수 하나로 두 모드가 공존**한다(듀얼모드). 덕분에 테스트 443개가 한 줄도 안 고친 채 그대로 살아남았고, 백엔드가 안 떠 있어도 개발이 계속된다.

- wiki-front: `backend-integration` 브랜치 8태스크 → main 머지 `485d93b`, 443 green
- 이어서 07-22에 백엔드 모드 화면 정합화(`a4c6c9f`) + dev 프록시(`d20a2be`) → 445 green
- wiki-backend: org-service 불능 시 **503 전파**(`5d39c50`), 30 green
- 원칙: **스토어 시그니처 불변** — 화면·테스트는 자기가 어느 모드에서 도는지 모른다

---

## 1부. 무엇을 했나 — 쉽게 설명하면

### 듀얼모드: 스위치 하나로 두 개의 뒷단

프론트의 `wikiStore`는 화면이 데이터를 요청하는 **단일 창구**다. `listSpaces()`, `createPage()`, `updatePage()` 같은 함수 이름과 의미가 계약으로 고정돼 있다.

이번에 한 일은 그 창구 뒤에 **구현체를 하나 더** 놓은 것이다.

```
화면(React)  →  wikiStore(계약 고정)  ┬─ wikiMock.ts    (localStorage)
                                      └─ wikiApi.ts     (fetch → wiki-backend)
```

어느 쪽을 쓸지는 빌드 시점 환경변수가 정한다:

```ts
export const USE_BACKEND =
  Boolean(import.meta.env.VITE_API_PROXY) || Boolean(import.meta.env.VITE_API_BASE);
```

- **아무것도 설정 안 함** → 목업. 테스트·CI·오프라인 개발이 여기 산다.
- **`VITE_API_PROXY` 설정** → dev 서버가 `/api`·`/oauth2`·`/login`을 게이트웨이로 프록시. 브라우저 입장에선 same-origin이라 **CORS가 아예 발생하지 않는다** — 프로덕션 nginx([11번](/tech/notes/11))와 동일한 모양이다.
- **`VITE_API_BASE` 설정** → 절대 URL로 직접 크로스-오리진 접속(게이트웨이 CORS + 쿠키 SameSite 설정이 별도로 필요).

> **[NOTE] 왜 목업을 안 지웠나**
>
> 목업을 지우면 테스트 443개가 전부 "백엔드가 떠 있어야 통과하는 테스트"가 된다. CI에 Postgres·Keycloak·org-service·게이트웨이를 다 띄우거나, 아니면 테스트를 fetch 모킹으로 전부 다시 쓰거나 — 둘 다 비싸다. 목업을 남기면 **마이그레이션 리스크가 백엔드 모드 안에 격리**된다.

### 경계 매퍼: 두 세계의 말이 다르다

백엔드와 프론트는 같은 개념을 다르게 부른다. 이 번역을 화면 곳곳에 흩어두면 나중에 필드 하나 바뀔 때 전수조사를 해야 한다. 그래서 **순수 함수 한 파일**(`store/mapping.ts`)에 몰아넣었다 — 부수효과 없음, 그래서 단위 테스트가 쉽다.

| 개념 | 백엔드 | 프론트 | 경계에서 하는 일 |
|---|---|---|---|
| id | `Long`(숫자) | `string` | `String(id)` ↔ `Number(id)`. 정수가 아니면 즉시 throw |
| 페이지 본문 | `content` | `body` | 이름만 바꿔 옮김 |
| 페이지 버전 | `version: int` | (없었음) | `Page.version` 신설 → 낙관적 락의 재료 |
| 형제 순서 | **없음** | `position` | 서버에 안 남는다(아래 참조) |
| 버전 이력 | `editedBy`/`createdAt` | `savedBy`/`savedAt` | 필드 매핑 |
| 에러 | `{ "error": "..." }` + 상태코드 | 한국어 예외 | 응답 `error`를 한국어 문구로, 없으면 상태코드별 폴백 |

**id 타입이 다른 게 왜 문제냐**면, JS에서 `"12" === 12`는 거짓이기 때문이다. 트리에서 부모를 찾을 때 `parentId === page.id`로 비교하는 코드가 한 군데라도 숫자와 문자열을 섞으면 그 노드는 조용히 트리에서 사라진다. 그래서 **프론트 타입은 전부 string으로 통일**하고, 백엔드로 나가는 순간에만 숫자로 되돌린다.

### 낙관적 락을 화면 변경 없이 통과시키기 — 그리고 그 대가

백엔드는 `PUT`에 `expectedVersion`을 요구하고, 안 맞으면 **409 Conflict**를 낸다([17번](/tech/notes/17)). 문제는 프론트의 `updatePage(id, patch)` 시그니처에 version 자리가 없다는 것이었다.

"화면 무수정"을 지키려고 어댑터가 **저장 직전에 `getPage`로 현재 version을 읽어서** `expectedVersion`에 실었다. 동작은 한다. 그런데 이게 **낙관적 락의 의미를 없앤다**:

- 낙관적 락의 목적은 "내가 **편집을 시작할 때** 본 버전"과 "지금 서버 버전"이 다르면 막는 것이다.
- 어댑터가 읽는 건 **저장 시점의 최신 버전**이다. 그 사이 남이 저장했어도 최신값을 그대로 실어 보내니 서버는 항상 통과시킨다 → **last-write-wins**(나중에 저장한 사람이 남의 편집을 덮어씀).

이건 최종 리뷰에서 발견돼 **알려진 한계로 기록**됐다(설계 §9-1). 진짜로 고치려면 편집 화면이 로드 시점의 version을 들고 있다가 넘겨야 하는데, 그건 "화면 무수정" 스코프 밖이라 후속으로 뺐다.

> **[TIP] 여기서 배운 것**
>
> "화면을 안 고치겠다"는 제약은 **어댑터가 정보를 지어내게 만든다.** 없는 정보(로드 시점 version)를 어댑터가 대신 만들어내면, 계약은 만족하지만 기능은 죽는다. 계약을 지킨 것과 목적을 달성한 것은 다르다.

### 형제 순서는 서버에 안 남는다

백엔드 트리는 `{id, parentId, title}`만 준다 — `position`이 없다. 페이지 이동도 `PUT`으로 `parentId`만 바꾼다. 그래서 **드래그로 순서를 바꿔도 서버엔 부모 변경만 남고 순서는 안 남는다.** 이건 숨기지 않고 결정으로 명시했다(설계 §7-4): 수용하고, `wiki-backend`에 `position` 컬럼 추가는 후속 티켓.

### 없는 것들: 사용자 이름, 댓글

- **사용자 이름/아바타** — wiki-backend에 User 테이블이 없다. JWT의 `sub`는 숫자 id고, 이름은 org-service가 안다. 현재 사용자만 `/api/me`로 이름을 얻고, 나머지는 `사용자 #{id}` 폴백. org-service의 users API 노출은 후속.
- **댓글** — 백엔드에 없다. localStorage 목업을 **별도 모듈로 분리해서 유지**한다. 백엔드에서 온 페이지 id를 키로 쓰므로, 백엔드 데이터와 목업 댓글이 한 화면에 공존한다.

### 503 전파 — "비어 있음"과 "고장남"은 다르다

이게 이번 작업에서 가장 값진 발견이다.

wiki-backend는 권한을 스스로 판정하지 않고 org-service에 gRPC로 물어본다. 그리고 org-service가 죽으면 **fail-closed**(모르면 거부)로 동작한다 — 인가에서는 옳은 기본값이다.

그런데 목록 API에서 이게 이상하게 나타났다. `accessibleSpaces`가 "권한 확인 실패 → 접근 가능한 스페이스 없음 → **빈 배열**"을 반환했다. 프론트가 받는 건 `[]`다. 화면은 "스페이스가 하나도 없네요"라는 **정상 빈 상태**를 그린다.

**사용자는 자기 문서가 전부 사라졌다고 본다.** 실제로는 권한 서비스가 죽었을 뿐인데.

```
org-service 다운
   → gRPC UNAVAILABLE
      → (전) 빈 배열 반환 → 프론트 "스페이스가 없습니다"   ← 거짓말
      → (후) 503 ServiceUnavailable → 프론트 "일시적으로 불러올 수 없습니다 [다시 시도]"
```

수정은 **장애의 종류를 구분**하는 것이었다(`5d39c50`):

- `UNAVAILABLE` · `DEADLINE_EXCEEDED` = **전송 장애**(상대가 없거나 못 닿음) → `ServiceUnavailableException` → **503으로 전파**. "판정을 못 했다"는 사실을 위로 올린다.
- 그 외(`INTERNAL` 등) = **판정은 했는데 뭔가 잘못됨** → 기존대로 fail-closed(거부).

프론트도 같이 고쳤다(`a4c6c9f`). `App`이 `listSpaces` 실패를 빈 목록으로 삼키지 않고 **에러 화면 + "다시 시도"**로 노출한다.

> **[IMPORTANT] 일반화하면**
>
> **fail-closed와 "조용한 빈 결과"는 다른 것이다.** 단일 리소스 조회에서 fail-closed는 403이 되어 사용자에게 보인다. 하지만 *목록* API에서 fail-closed를 빈 배열로 구현하면, 실패가 **성공적인 빈 상태로 위장**된다. 목록을 반환하는 모든 권한 필터링 지점에서 이 함정을 확인할 것.

---

## 2부. 과정 — 어떻게 진행됐나

### 태스크 순서 (wiki-front `backend-integration`, 8태스크)

각 태스크는 "구현 → 리뷰 → (지적 반영) → 커밋" 사이클로 돌았다.

| # | 내용 | 결과 |
|---|---|---|
| 1 | auth 클라이언트 싱글톤 공유 + 듀얼모드 스위치 | `apiClient.ts` |
| 2 | 경계 매퍼 `mapping.ts` + `Page.version`·`Space.description` 추가 | 리뷰 clean |
| 3 | spaces 어댑터 | 리뷰가 "테스트가 호출 구조를 검증 안 함"을 지적 → 보강 후 통과 |
| 4 | pages 어댑터(+낙관적 락) | `getPage`→`PUT expectedVersion`→409 매핑 검증 |
| 5 | versions 어댑터(리비전·복원) | 복합 id 파싱 end-to-end 검증 |
| 6 | users 폴백 + comments 목업 유지 | `사용자 #{id}` |
| 8 | attachments 어댑터 | FormData에 Content-Type 수동 지정 안 함(경계 확인) |
| 7 | `.env.example` + `.gitignore` | 수동 스모크는 사용자 몫 |

**AuthGate가 프로덕션 전용이라 dev에서 토큰이 없다**는 문제가 있었다. 해법은 "가짜 dev 토큰"이 아니라 **백엔드 모드일 때만 dev에서도 AuthGate를 켜는 것**이었다. 이유: 권한이 org-service fail-closed라 **실제 grant가 있는 진짜 사용자**여야 목록이 보인다. 가짜 토큰으로는 어차피 아무것도 못 본다.

### 최종 리뷰가 잡은 것 — 전부 백엔드 모드 전용

브랜치 전체 리뷰 판정은 "With fixes". 목업 모드는 블로커 0건(443 green, 코드가 verbatim 이동했음이 확인됨). Important 3건은 모두 **`VITE_API_BASE`를 설정했을 때만 나타나는** 것들이었다:

1. **낙관적 락 무력화** (위 §1부)
2. **"Invalid Date"** — 백엔드 `SpaceResponse`·`PageResponse`에 시각 필드가 없어 매퍼가 `""`를 넣는다. 화면이 `new Date("").toLocaleDateString()`을 부르면 `Invalid Date`가 그대로 렌더된다.
3. **복원이 항상 "변경 없음"** — HistoryModal의 no-op 판정이 `restored.updatedAt === page.updatedAt`인데 백엔드 모드에선 둘 다 `""`라 항상 참. 실제로 복원됐는데 "현재 내용과 동일합니다" 토스트가 뜬다.

이 브랜치는 "화면 무수정" 스코프였으므로, 리뷰는 **코드 대신 문서를 고치게** 했다: 매퍼의 주석에 허위 주장(실제로 하지 않는 `"-"` 표기)을 제거하고, 설계 문서에 §9 "Backend-mode 알려진 한계"를 신설했다.

### 이어서 07-22 — 미룬 것을 실제로 갚았다

머지 직후 세 개의 후속 커밋으로 §9를 정리했다.

**`d20a2be` — dev 프록시로 CORS 회피**
- `VITE_API_PROXY` 신설. dev 서버가 `/api`·`/oauth2`·`/login`을 게이트웨이로 프록시 → same-origin.
- 백엔드 토글을 base URL과 **분리**했다(`USE_BACKEND = VITE_API_PROXY | VITE_API_BASE`). 클라이언트 `baseUrl`은 상대경로를 유지한다.
- **vitest가 로컬 `.env`를 읽어 백엔드 모드로 돌던 문제**를 `import.meta.env` 오버라이드로 고정 — 테스트는 항상 목업 모드.

> **[WARNING] 이 함정은 재발한다**
>
> `.env`에 백엔드 모드를 켜두면 **dev 서버뿐 아니라 vitest도** 그 값을 읽는다. 그러면 "로컬에서만 테스트가 깨지는" 상황이 된다. 테스트 설정에서 모드를 명시적으로 고정해야 한다. (관련: wiki-front-mock-mode-dev — `.env`가 백엔드 프록시라 목업 검증은 `VITE_API_PROXY= pnpm dev --port 5199`로 띄운다.)

**`a4c6c9f` — 백엔드 모드 화면 정합화 (§9 해소)**
- `formatDate`/`formatDateTime`에 빈값·무효 날짜 가드 → "Invalid Date" 소멸(디렉토리는 `-`).
- `displayUserName(lib/userName)` — 이름이 없고 id만 있으면 `사용자 #{id}`.
- PageView 메타: 작성자·수정일이 **둘 다** 없으면 블록 자체를 숨긴다(빈 껍데기 표시 금지).
- HistoryModal 복원 no-op 판정: `updatedAt`이 빈 문자열이면 판정을 건너뛴다 → 백엔드 restore는 항상 "복원했습니다".
- `App`: `listSpaces` 실패를 에러 화면 + 다시 시도로 노출(위 503 이야기).
- 테스트 `App.w8-backend-mode` 신설 — 503 에러 상태와 빈 시각 메타 숨김을 고정. **445 green.**

**`5d39c50`(wiki-backend) — 503 전파**
- `ServiceUnavailableException` + `ApiExceptionHandler` 503 매핑.
- `GrpcPermissionClient`의 `accessibleSpaces`/`isAllowed`에서 `UNAVAILABLE`·`DEADLINE_EXCEEDED`만 전파, 나머지는 fail-closed 유지.
- 테스트로 **두 분기를 구분해서** 고정(전파 vs fail-closed). 30 green.

---

## 3부. 남은 것

| # | 항목 | 성격 |
|---|---|---|
| 1 | **load-time version 낙관적 락** — 편집 화면이 로드 시점 version을 들고 저장에 전달 | 기능 결손(화면 변경 필요) |
| 2 | `wiki-backend`에 `position` 컬럼 — 형제 순서 영속 | 백엔드 계약 확장 |
| 3 | org-service **users API** 노출 → `사용자 #{id}` 폴백 대체 | 백엔드 계약 확장 |
| 4 | 백엔드 **comments** — 현재 localStorage 목업 | 백엔드 신규 |
| 5 | `Space.key` 대소문자 — 프론트 문서는 대문자, 백엔드는 소문자 passthrough | 계약 정합 |
| 6 | `/api/me` 중복 호출(AuthGate + getCurrentUser) dedupe | Minor |
| 7 | **백엔드 모드 수동 스모크** — 플랫폼 기동 + 실제 Keycloak 로그인 후 스페이스 생성→트리→편집→버전 왕복 | 사용자 실행 |

> **[NOTE] 이 문서 이후**
>
> 콘텐츠 모델(`Page.type` 폴더 / `Page.status` 초안)은 이 시점엔 아직 없었다. 07-26에 목업 모드 선행으로 확정되며, 백엔드 계약 확장이 새로 대기열에 올랐다 → [22 wiki-front 컨플루언스 복제 리뉴얼 + 폴더·초안·컬럼 콘텐츠 모델 (2026-07-26)](/tech/notes/22)

---

## 부록. 이 문서의 근거

- wiki-front: `485d93b`(머지) · `d20a2be` · `a4c6c9f` · `ae6f905`(머지)
- wiki-backend: `5d39c50` · `93cb89c`(머지)
- 설계: `wiki-front/docs/superpowers/specs/2026-07-21-backend-integration-design.md` (§7 결정 / §9 알려진 한계)
- 플랜: `wiki-front/docs/superpowers/plans/2026-07-21-backend-integration.md`
- 진행 원장: `wiki-front/.superpowers/sdd/progress.md` (Backend Integration 섹션)
