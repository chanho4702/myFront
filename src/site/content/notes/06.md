상위: [00 개요 — 전체 구조](/tech/notes/00)

## 저장소 구조 (각 서비스 = 별도 repo)

| 컴포넌트 | repo | 비고 |
|---|---|---|
| 인증 서버 | `github.com/chanho4702/auth-server` | 자체 repo, 우산에선 gitignore |
| 프론트 | `ch-stack-frontend.git` (branch `feat/keycloak-oidc-login`) | myFront |
| 백엔드 샘플 | `backend-server.git` (main) | board-service |
| keycloakify 테마 | keycloakify-starter fork | platform-react 테마 소스 |
| **우산 템플릿** | `infra-settings.git` (main) | = `C:\MSA_TEMPLATE`, **인프라(keycloak)+구조만** |

우산(`infra-settings`) 트리: `infra/`(keycloak compose·realm·테마 jar) + `.gitignore` + `.superpowers`. **board-service·docs는 gitignore로 제외.**

## 2026-06-30 정리 작업 (이 세션)

- 갈라져 있던 board-service(완성본)를 정리: 처음 master에 통합했다가 → 사용자 의도대로 **별도 repo(backend-server.git)로 분리**, 우산에서 gitignore.
- 잡브랜치(`feat/board-service`, `bs-export`, `feat/keycloakify-login-theme`) + 워크트리(`C:/bs-wt`) 삭제.
- 우산 master를 `infra-settings/main`에 정리 push(인프라+구조만, docs 제거).
- 3개 repo push 완료: backend-server / ch-stack-frontend / infra-settings.

## 운영 원칙

### 문서는 Obsidian에만 (예외 없음)
- 사용자 지시(2026-06-30): **"앞으로 문서 쓸 일 있으면 무조건 Obsidian 경로에."**
- 모든 설계/스펙/플랜/작업기록 .md는 **`C:\myBrain\내 로컬`(Obsidian, repo `chanho4702/Obsidian`)** 에만 작성. 어떤 repo의 `docs/`에도 새 문서 금지.
- 강제장치: 우산 repo에 **`/docs/` gitignore** 추가(docs가 인프라 repo로 새어나가는 것 차단).
- brainstorming/writing-plans 스킬이 `docs/superpowers/...`에 쓰라고 해도 → Obsidian으로 우회.

### 서비스/연관 구분
- **코드 연관 0**: 각 서비스는 서로의 소스를 import하지 않음.
- **실행 시점 SSO 연관(정상)**: board-service → auth-server JWKS(설정값 한 줄). 이건 의도된 MSA 연결이라 유지.

## 남은 과업

- T1: keycloakify 테마 브라우저 시각확인 + 구글 실로그인 (사람 항목) → [02 keycloakify 로그인 테마](/tech/notes/02)
- 프론트 게시판 브라우저 클릭 E2E (사람 항목) → [04 프론트 연동 (myFront + board-service)](/tech/notes/04)
- ~~T3: API 게이트웨이 구현~~ → 완료(2026-07-01, gateway-server :8000) [05 API 게이트웨이 설계 (구현 완료)](/tech/notes/05)
- ~~운영 하드닝(dev-안전 분량)~~ → 완료(2026-07-02): aud/iss 검증·시크릿 env 외부화·Secure쿠키 설정화·KC brute-force 등 [07 보안 감사 + 하드닝 (2026-07-02)](/tech/notes/07)
- (운영 전) HTTPS/TLS·JWK 키회전·rate limiting·RT 정리 배치 → [07 보안 감사 + 하드닝 (2026-07-02)](/tech/notes/07) 로드맵 참고.
- (선택) 댓글 UI 프론트 연결.

← 처음으로: [00 개요 — 전체 구조](/tech/notes/00)
