상위: [00 개요 — 전체 구조](/tech/notes/00) · 선행: [05 API 게이트웨이 설계 (구현 완료)](/tech/notes/05)(구현), [07 보안 감사 + 하드닝 (2026-07-02)](/tech/notes/07)(로드맵 1·3·6번 이행)

"게이트웨이가 하는 일이 너무 없다(라우팅+CORS뿐)"는 문제의식에서 출발 — 얇은 프록시를 실질적 API 게이트웨이로. 플랜: `plans/2026-07-03-gateway-traffic-auth.md`, TDD 인라인 실행.

## 추가된 역할 (커밋별, gateway-server repo)

| 커밋 | 내용 |
|---|---|
| `b24cea7` | **전역 타임아웃** — connect 3s / response 10s. 다운스트림 다운·지연 시 무한대기 방지 |
| `cd02492` | **Rate limiting** — IP 기준(`ipKeyResolver`), `/api/auth/**` 5 req/s(burst 10), `/oauth2/**`·`/login/**` 10/20. Redis 백엔드(infra `b7b0366`에 redis:7-alpine 추가), **부재 시 fail-open** |
| `af94463` | **CircuitBreaker + Retry + fallback** — board 라우트. 불능 시 `/fallback/board` 503 `{"error":"board_unavailable"}`. Retry는 멱등 GET 한정 2회 |
| `2709e8c` | **JWT 조기차단** — reactive resource server. 보호 경로 무토큰/위조를 다운스트림 전에 401. JWKS+issuer+audience 검증(board와 동일 계약). **CORS를 yml globalcors → Security 빈 단일화** |
| `e138c39` | 최종리뷰 수정(아래) + README 현행화 + .gitignore .idea/ |

## 아키텍처 결정 2가지 (중요)

**1. 이중 검증(zero-trust)** — 게이트웨이 검증은 1차 방어(쓰레기 트래픽 조기 차단)일 뿐, board-service·auth-server의 자체 JWKS 검증(최종 방어)을 대체하지 않는다. 내부망에서 게이트웨이 우회 직접 호출이 있어도 안전.
→ **동기화 규칙**: board-service에 공개 경로를 추가하면 gateway `SecurityConfig`에도 추가해야 한다(게이트웨이가 더 좁으면 먼저 401을 내버림).

**2. CORS를 Security 빈으로 이전한 이유** — Security가 내는 **401 응답에도 CORS 헤더가 붙어야** 브라우저의 fetch가 상태코드를 읽고 myFront의 401→refresh 흐름이 산다. yml globalcors는 라우팅 단계라 Security 401에는 헤더를 못 붙인다. 회귀 테스트(`unauthorizedResponseCarriesCorsHeaders`)로 고정.

## 최종 리뷰에서 잡힌 실동작 결함 (교훈)

- **C1 (Critical)**: CircuitBreaker를 달면 **Resilience4j 기본 TimeLimiter 1초**가 라우트 전체를 캡한다 — 1초 넘는 '정상' 응답까지 fallback 503으로 잘리고, 선언해둔 response-timeout 10s가 무효가 됨. 1.5초 지연 정상 스텁(JDK HttpServer) 테스트로 RED 재현 → `resilience4j.timelimiter.instances.board.timeout-duration: 11s`로 수정. **CB 도입 시 TimeLimiter 명시는 필수 체크리스트.**
- I1: Retry 기본 exceptions에 TimeoutException이 포함돼 있어 타임아웃도 재시도 → fallback 지연 3배 증폭 가능. `exceptions: java.io.IOException`(연결 실패만)으로 한정.
- I2: Redis 없이 돌리면 rate limit 경로 요청마다 `Error calling rate limiter lua` **ERROR 로그가 남는 게 정상**(fail-open) — README에 명시.

## 테스트 (전체 green, 다운스트림·Redis 없이 실행 가능)

RouteConfigTest(+rate limiter 필터 존재) / CorsTest / SecurityConfigTest(401 조기차단·401+CORS·공개경로 통과) / BoardFallbackTest / SlowBoardDownstreamTest(TimeLimiter 회귀) / HttpClientTimeoutTest / IpKeyResolverTest / RequestLoggingFilterTest

## 수동 검증 (남음)

- `cd infra/keycloak && docker compose up -d` (redis 포함) → 5서비스 기동 → 브라우저 로그인 E2E.
- rate limit 실동작: `for i in $(seq 1 15); do curl -s -o /dev/null -w '%{http_code}\n' -X POST localhost:8000/api/auth/refresh; done` → 뒤쪽 429.
- 유효 토큰으로 `POST :8000/api/board/posts` → 201 (게이트웨이+board 이중 검증 통과).

## 남은 확장 포인트

서비스 디스커버리(lb://), Micrometer Tracing(OTel — 수동 X-Request-Id 대체), 요청 크기 제한·보안 응답 헤더. 운영 전 필수는 [07 보안 감사 + 하드닝 (2026-07-02)](/tech/notes/07) 로드맵(HTTPS·키회전·시크릿 매니저·RT 정리 배치) 참고.

← 처음으로: [00 개요 — 전체 구조](/tech/notes/00)
